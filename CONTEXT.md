# 板上重排

校内白板要现场投屏资料，而资料自身的排版不适合直接展示。这个上下文定义的是
「把若干资料拆成区域、在无限画布上重新编排，再呈现到大屏」这件事里的语言。

场景范围：**上课前临时把资料摆好、投出去、用完就关**——不是讲课工具，
不承担授课过程中的操作（老师要讲课用希沃白板）。因此这个产品**只解决重排**，
不做留存、不做检索、不做导航。它是转化器，不是知识库。

## Language

**资源（Artifact）**:
一份被投放进来的原始文件，例如一份 PDF 试卷或一张照片。
它只服务这一轮管线：栅格化 → 解析 → 编排 → 投屏，走完就散。
**栅格化之后不为换分辨率而保留原件**（[ADR-0011](docs/adr/0011-no-rollback-rasterize-once.md)）。
_Avoid_: 文档、文件、素材、课件

**栅格化（Rasterize）**:
把非图片格式的资源（PDF、DOCX）整页转成位图的过程，是解析的前置步骤，不抽取任何内容。
_Avoid_: 转图、渲染、截图、图片化

**解析（Parse）**:
从一份资源中切分出区域的过程。资源在解析前一律已被栅格化，因此解析只处理位图。
_Avoid_: 识别、提取、分析、预处理

**拍板（Confirm）**:
操作者对 AI 给出的裁切建议做出接受或修改的决定，是解析结果的生效点。
_Avoid_: 确认、审核、校对、review

**区域（Region）**:
栅格图上裁切出的一块位图内容，在裁切（及拍板）那一刻诞生，是画布上的最小摆放单位，
可以来自任意资源。
_Avoid_: 块、片段、切片、元素

**语义单元（Semantic Unit）**:
指几块区域合起来构成一个完整意思的单位（一道题、一个表格）。
由 AI 在解析时**提议**，操作者可手工拆开或合并；是一个提议，不是定论。
_Avoid_: 题块、逻辑块、内容块

**碎开（Scatter）**:
解析产出区域后，整份资源在画布上「裂成若干块」的呈现：各块停在原位置附近、
彼此轻微分离但保留原有的相对位置。它是解析与编排之间的分界——碎开之前是资源的
版面，碎开之后是等着被摆的区域。
_Avoid_: 爆炸、散开、打碎

**画布（Canvas）**:
操作者自由摆放与裁剪区域的无限工作区，内容不局限于任何单一资源。
使用上是一次性的——讲完一份资源即清空；结构上支持多份资源的区域共存，
重跑解析时按资源归属精确清理。
_Avoid_: 白板、舞台、工作区、页面

**版面（Layout）**:
一个区域内部的排布，或一块资源原有的排布。指内容自身的组织形式，不指它在画布上
被摆在哪里。
_Avoid_: 布局、排版、样式

**编排（Arrangement）**:
操作者把区域摆到画布上合适位置的过程与结果。是**纯人工**资产——AI 只负责拆分，
从不摆放；与之相对的是可重算的解析结果。
_Avoid_: 设计、编辑、调整、自动排版

**决策模型（Decision Model）**:
负责版面检测与区域分割的专用视觉模型。它不读字，只判断「哪里是一块内容」
以及「哪几块属于同一道题」。
_Avoid_: AI、视觉模型、OCR

**投屏（Cast）**:
把画布呈现到大屏上供现场观看。
_Avoid_: 投影、投送、放映、屏幕镜像

**操作者（Operator）**:
站在大屏前操作的人，通常是授课老师。
_Avoid_: 用户、老师、观众
